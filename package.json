{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-cv.js",
    "build:site": "astro build",
    "generate:cv": "node scripts/generate-cv.js",
    "preview": "astro preview"
  },
  "type": "module",
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^6.1.5",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}